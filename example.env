# Copy to `.env` (or use via systemd EnvironmentFile) and edit values.
# NOTE: Do NOT commit real `.env` files.

# Django
DJANGO_SECRET_KEY="change-me"
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Redis (local dev)
REDIS_URL=redis://127.0.0.1:6379/0

# Instance identity (for EC2 diagnostics; local dev can use any value)
INSTANCE_ID=local-dev

# Optional DB (leave unset if you don't need a database)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

